models:
  BotExternalRequest:
    bot_id: id foreign:bots
    bot_user_id: id foreign:bot_users
    command: nullable string:255
    completed_at: timestamp nullable
    relationships:
      belongsTo: BotUser, Bot



seeders:   BotExternalRequest

controllers:
  BotExternalRequest:
    resource: api
