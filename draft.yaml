models:
  BotWarning:
    bot_id: id foreign:bots
    rule_key: string:50
    rule_value: integer default:0
    is_active: boolean default:false
    relationships:
      belongsTo: Bot



seeders:   BotWarning

controllers:
  BotWarning:
    resource: api
