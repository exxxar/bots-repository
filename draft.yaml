models:
  JoinChannel:
    channel_id: string:255 nullable
    telegram_chat_id: string:255 nullable
    bot_id: id foreign:bots
    joined_at: timestamp nullable
    left_at: timestamp nullable
    relationships:
      belongsTo: Bot

seeders:  JoinChannel

controllers:
  JoinChannel:
    resource: api
