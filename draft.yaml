models:
  Basket:
    product_id: id foreign:products
    count: integer default:1
    bot_user_id:  id foreign:bot_users
    bot_id: id foreign:bots
    ordered_at: timestamp nullable
    relationships:
      belongsTo: Bot, BotUser, Product

seeders: Basket

controllers:
  Basket:
    resource: api
